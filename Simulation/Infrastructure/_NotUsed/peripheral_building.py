import Utilities.sim_input_processing as prep


# NOT USED!
class PeripheralBuilding:

    def __init__(self, env, building_type, load_profile):
        self.env = env
        self.sim_start_day = sim_start_day
        self.sim_duration = sim_duration
        self.building_type = building_type  # office building, factory
        self.load_profile = load_profile
        self.num_lookback_periods = num_lookback_periods

        # TODO!
        def update_load_profile(self):

            xxxx
